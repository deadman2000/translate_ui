export type IReplace = {
    id: string,
    volume: string,
    number: number,
    src: string,
    text: string,
    replaced: string
}

export type IReplaceMode = {
    mode: string,
    desc: string
}